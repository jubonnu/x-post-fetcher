# Worker（apps/worker）
INGEST_TOKEN=dev-secret-token
TURSO_DATABASE_URL=file:local.db
# TURSO_AUTH_TOKEN=...        # 本番Tursoのみ
# PORT=8787

# Scraper ジョブ（apps/scraper: npm run scrape）
INGEST_URL=http://localhost:8787/ingest
# INGEST_TOKEN は Worker と同じ値を使う
TARGET_USER=Zabi_pokeka
MAX_POSTS=14

# 解析は 100% ルールベース。LLM / API キーは使用しない。
