node_modules/
dist/
auth.json
output/*.html
output/posts.json

# libSQL ローカルDB / テスト一時ファイル
*.db
*.db-wal
*.db-shm
.tmp-test-*
local.db

# env
.env
.env.local

# Apple秘密鍵（Sign in with Apple / token exchange用）。原則リポジトリ外で管理する。
*.p8
AuthKey_*.p8
